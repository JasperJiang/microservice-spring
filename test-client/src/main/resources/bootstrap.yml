spring:
  application:
      name: test-client

app:
  oauth2:
    accesstokenUrl: http://localhost:9999/uaa/oauth/token
    common:
      clientId: test-service
      secret: test123
      scope: operation

feign:
  hystrix:
    enabled: false
---

spring:
  cloud:
    config:
      uri: http://localhost:8888